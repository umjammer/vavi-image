/*
 * Copyright (c) 2022 by Naohide Sano, All rights reserved.
 *
 * Programmed by Naohide Sano
 */

package vavi.imageio;

import java.io.IOException;


/**
 * WrappedImageInputStreamTest.
 *
 * @author <a href="mailto:umjammer@gmail.com">Naohide Sano</a> (nsano)
 * @version 0.00 2022-09-11 nsano initial version <br>
 */
public class WrappedImageInputStreamTest {

    void test1() throws IOException {
//new Exception("*** DUMMY ***").printStackTrace();
//Debug.println(Level.FINE, "is.length(): " + is.length());
//Debug.println(Level.FINE, "is.getStreamPosition(): " + is.getStreamPosition());
//        if (length == -1) {
//Debug.println(Level.FINE, "ImageInputStream: " + is.getClass().getName());
//Debug.println(Level.FINE, "is: " + StringUtil.paramString(is));
//            if (is instanceof FileCacheImageInputStream) {
//                try {
//                    FileCacheImageInputStream fciiis = (FileCacheImageInputStream) is;
//                    Field field = BeanUtil.getFieldByNameOf(FileCacheImageInputStream.class, "stream");
//                    InputStream stream = (InputStream) BeanUtil.getFieldValue(field, fciiis);
//                    length = stream.available();
//                } catch (Exception e) {
//                    e.printStackTrace();
//                }
//Debug.println(Level.FINE, "length: " + length);
//                return (int) length;
//            }
//        }
    }
}
